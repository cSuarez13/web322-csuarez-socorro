<div class="min-h-screen flex flex-col justify-center sm:px-5">
    <div class="relative py-3 px-6 :max-w-xl sm:mx-auto">
        <div
            class="absolute inset-0 bg-gradient-to-r from-amber-500 to-lime-400 shadow-lg transform -skew-y-6 
            sm:skew-y-0 sm:-rotate-6 sm:rounded-3xl">
        </div>
        <div class="text-white relative px-6 py-12 bg-gradient-to-l from-amber-300 to-white shadow-lg sm:rounded-3xl sm:p-20">

            <div class="text-center pb-8">
                <h1 class="text-4xl font-bold text-green-900"> Are you sure you want to delete <span class="text-green-500 drop-shadow-md"><%= title %></span>?</h1>
            </div>

            <div class="flex justify-between mt-6">
                <form id="confirmationForm" method="post" action="/mealkits/remove/<%= ID %>" class="w-1/2 mr-4">
                    <button type="submit" id="yesButton"
                    class="w-full rounded-md border border-transparent bg-green-500 py-3 px-6 font-semibold    
                    text-3xl text-white shadow-lg hover:bg-green-600 focus:outline-none focus:ring-2 
                    focus:ring-green-500 focus:ring-offset-2">Yes
                </button>
                </form>
                <button type="button" id="noButton" onclick="window.location.href='/mealkits/list'"
                    class="w-1/2 rounded-md border border-transparent ml-4
                    bg-amber-500 py-3 px-6 font-semibold text-3xl text-white shadow-lg 
                    hover:bg-amber-600 focus:outline-none focus:ring-2 focus:ring-amber-500 
                    focus:ring-offset-2">No
                </button>
            </div>
            
        </div>
    </div>
</div>